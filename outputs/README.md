# Generated outputs

This folder contains generated experiment artifacts (CSV/Markdown summaries and plots).

These files are intentionally **not tracked** in git to avoid recurring merge conflicts with `main`.
Re-generate them by running:

```bash
python -m src.thesis_repro.run_pipeline --sample-size 60000
```
